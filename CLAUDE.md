# Dinner Planner

Private, two-person meal-planning web app for Adam and his partner (Android to follow later). Full spec, data model, screens, and phased build plan: see `docs/BUILD_PLAN.md` — read that before writing code.

## Quick facts

- **Stack**: React + TypeScript + Vite, Tailwind CSS, Firebase (Auth + Firestore + Hosting) on the free Spark plan. No Cloud Functions — everything client-side + security rules.
- **Access control**: Google SSO, restricted to whoever has a document in the Firestore `users` collection (doc ID = their email, `name` field = explicit display name — not pulled from their Google profile). This collection *is* the allowlist — `firestore.rules` checks `exists()` on `users/{email}`. No in-app admin UI on purpose: Adam manages membership directly in the Firebase console (add/remove a document) rather than editing code and redeploying.
- **Permissions**: fully symmetric between the two accounts — no owner/admin distinction.
- **Day model**: a day has either a planned meal, or one/both people marked "not home" with a reason — never both. Not-home means no meal is needed that day at all.
- **Proposals**: dropped from scope. Built twice (a standalone batch screen, then folded into the calendar's day editor) and removed both times — Adam found it too confusing. Manual day assignment (no approval step) is the only way to set a day's meal. See `docs/BUILD_PLAN.md` §1/§6 for the history if this ever comes back up.
- **Android (later, not now)**: same web build wrapped with Capacitor into a sideloaded APK — no Play Store. Push notifications later via FCM through Capacitor's plugin.
- Protein add-ons (chicken for partner / fake meat for Adam) are intentionally **not** modeled — out of scope.

## Workflow

- Work in phases as listed in `docs/BUILD_PLAN.md` §6, one branch per phase (e.g. `build/phase-0`, `build/phase-1`), opening a PR for Adam to review rather than pushing straight to `main`, unless he explicitly says otherwise for a given change.
- CI/CD (GitHub Actions, `.github/workflows/firebase-hosting-*.yml` — generated by `firebase init hosting:github`, then hand-edited to add lint + Firestore rules deploy): opening/updating a PR deploys a temporary Firebase Hosting preview channel for that PR — this shares the same live Firestore database/rules as production (no separate staging project), so treat data written while reviewing a preview as real. Merging to `main` deploys to the live Hosting channel and redeploys `firestore.rules`/`firestore.indexes.json`. Both use the `FIREBASE_SERVICE_ACCOUNT_DINNER_4BFA2` repo secret, a service account the `hosting:github` wizard created and granted its own IAM roles — if CI ever gets new permission errors, fix IAM roles on that service account (Google Cloud Console → IAM → project `dinner-4bfa2`) rather than regenerating the secret.

## Status

- Phase 0 (project scaffold) merged to `main`: Vite + React + TS + Tailwind (v4, via `@tailwindcss/vite`) scaffold; `firebase` SDK client init in `src/lib/firebase.ts`; `firestore.rules` with the two-email allowlist structure; `firebase.json` / `.firebaserc` / `firestore.indexes.json` for Hosting + Firestore.
- Firebase project `dinner-4bfa2` is created (Auth, Firestore, Hosting enabled) and the pipeline is proven end-to-end: `firebase deploy --only firestore:rules,hosting` succeeded, live at https://dinner-4bfa2.web.app. The web app's Firebase config isn't a secret (it ships in the client bundle; access is controlled by `firestore.rules` + Auth, not by hiding this config) so it's hardcoded directly in `src/lib/firebase.ts` rather than via env vars.
- Access control was originally a hardcoded two-email array in both `firestore.rules` and a client-side `src/lib/allowlist.ts` — replaced by the Firestore `users` collection described above (see the Access control quick fact, and the status entry below). Before that switch, the live allowlist had `adam@avari.technology` + `adameldridge89@gmail.com` (Adam's own second account) so he could test both sides of the app during the build; the equivalent `users` documents need the same two emails seeded before sign-in works again post-migration.
- Phase 1 (auth & shell) merged to `main` and confirmed working live at https://dinner-4bfa2.web.app: `AuthProvider`/`useAuth` (`src/contexts/`) wraps Firebase `onAuthStateChanged`/`signInWithPopup` so disallowed accounts are signed out immediately with a message, not just rejected server-side; `SignInPage`, `AppShell` (header with email + sign-out), and a placeholder `HomePage` wired up via `react-router-dom` in `App.tsx`. Google had to be manually enabled as a Sign-in method provider in the Firebase console (was off, causing sign-in to fail immediately) — Adam enabled it and confirmed login now works. (The allowlist mechanism itself has since moved to Firestore — see the later status entry.)
- CI/CD merged to `main` (PRs #3 and #4) and fully working — confirmed green on the Actions tab for both PR-preview and merge-to-main deploys. Getting here took two IAM fixes: (1) the hand-picked "Generate new private key" service account lacked project-level permission to check/deploy Firestore (403 from `serviceusage.googleapis.com`), fixed by switching to a service account from `firebase init hosting:github` instead; (2) that wizard-created account was scoped only for Hosting, so the Firestore rules deploy step then failed with a 403 from `firebaserules.googleapis.com` — fixed by granting it the broad **Firebase Admin** IAM role directly (Google Cloud Console → IAM → project `dinner-4bfa2`) rather than chasing individual API scopes further. If CI ever gets a new permission error, check that role first.
- Phase 2 (meal repertoire) merged to `main` and confirmed working: `src/lib/meals.ts` has the Firestore CRUD (`subscribeToMeals`, `getMeal`, `createMeal`, `updateMeal`, `archiveMeal` — soft-delete via `active: false`, per the data model); `MealsPage` (search + list, live via `onSnapshot`) and `MealFormPage` (shared create/edit form with a dynamic ingredient list) wired up at `/meals`, `/meals/new`, `/meals/:id/edit`; nav links added to `AppShell`. Shipped with a bug: `subscribeToMeals`'s query (`where('active','==',true)` + `orderBy('name')`) needs a Firestore composite index — filtering and ordering on different fields isn't covered by Firestore's automatic single-field indexes, contrary to what was assumed while building it. Broke the meals list in production with a `failed-precondition` console error until fixed by adding the index to `firestore.indexes.json` (now the source of truth for any future Firestore indexes — add new composite indexes there rather than only clicking the Firebase Console's auto-generated link, so they're deployed by CI and reproducible) and deploying via the merge-to-main workflow.
- Phase 3 (calendar & manual day planning) merged to `main` and confirmed working: `src/lib/days.ts` has the Firestore access for the `days` collection (`assignMeal`, `setNotHome`, `clearDay`, `getDay`, plus live subscriptions for one day and a date range — `DayMeal` has no `source` field, since the only source is manual assignment now that proposals are dropped). `CalendarPage` (the `/` route) shows the 21-day rolling window grouped into "This week"/"Next week"/"In 2 weeks" cards, each row color-coded (green background = meal assigned, greyed out = someone not home). Clicking a row expands it inline (accordion, one at a time, animated) into an editor instead of navigating to a separate page — a `/day/:date` route and `DayDetailPage` existed briefly but were folded back in after Adam's feedback that navigating away felt excessive. The editor has no separate "Clear day" button (Save with nothing selected clears the day) and no reason field for not-home (just a checkbox per person — `NotHomeEntry.reason` stays in the type, always written as `''`, in case it's wanted back later). **Deviation from `docs/BUILD_PLAN.md`'s data model**: `notHome` is an array of `{email, name, reason}` instead of a map keyed by `uid` — a uid-keyed map needs the partner's Firebase uid client-side (not available without extra infra), and an email-keyed map breaks Firestore's dotted-path field targeting since emails contain literal dots.
- Phase 4 (proposals) was built twice, then fully removed at Adam's request ("too confusing") — see the Proposals quick fact above. Nothing proposal-related was ever merged to `main` (the PR was closed and its branch deleted), so there was no app code to revert; that cleanup pass was about `docs/BUILD_PLAN.md` and `CLAUDE.md` still describing it as planned/built.
- Access control moved from a hardcoded two-email array (in both `firestore.rules` and `src/lib/allowlist.ts`) to a Firestore `users` collection — merged to `main` and confirmed working live: `src/lib/allowlist.ts` deleted, replaced by `src/lib/users.ts` (`isAllowedEmail` — async, does a `getDoc` — and `subscribeToUsers`). `firestore.rules`' `isAllowedUser()` checks `exists(/databases/$(database)/documents/users/$(request.auth.token.email))` instead of an `in [...]` array; added `match /users/{email} { allow read: if isAllowedUser(); }` with no write rule (console-managed only, no in-app admin UI by design). `CalendarPage`'s `DayEditor`/`DayRow` take a live `users` list instead of the old `ALLOWED_EMAILS` constant, using each person's Firestore `name` field (not `user.displayName`) everywhere a name gets recorded or displayed. Adam's real accounts are seeded: `users/adameldridge89@gmail.com` (his real email — `adam@avari.technology` was only ever a throwaway test account, not his real one, contrary to earlier assumptions in this file) and `users/matildahambleplummer@gmail.com` (partner's real email — already switched over, no longer a placeholder). Hit one bug post-merge: `isAllowedEmail()` assumed `getDoc` always resolves and just checked `.exists()`, but `isAllowedUser()`'s `exists()` check means Firestore denies the read outright for an email with no document (not "resolves with exists()===false") — surfaced as an uncaught `FirebaseError` in the console. Fixed by treating a permission-denied error the same as "not allowed".
- UI polish pass (mobile header, branding, PWA install support) merged to `main`: fixed the header breaking on phone-width viewports; replaced the inline email+"Sign out" with a circular avatar button (first letter of email) that opens a top-right dropdown menu; made "Dinner Planner" visually distinct from the nav links (`text-lg font-bold tracking-tight`) and fixed baseline alignment between them (`items-baseline`, not `items-center`, when mixing text sizes in one flex row). Added installability: `vite-plugin-pwa` generates `manifest.webmanifest` + a Workbox service worker at build time (dev mode doesn't register the SW, only `vite preview`/production); new `public/pwa-192.png`/`pwa-512.png` icons and a matching `public/favicon.svg`, all a simple white "D" monogram on slate-800 (`#1e293b`) since there's no real logo yet. Prompted by this: Adam decided the planned native Android app (Phase 7, Capacitor-wrapped sideloaded APK) is no longer needed now that the PWA covers "installed on my phone" — see the Android quick fact and `docs/BUILD_PLAN.md` §6.
- Next step: Adam tests "Add to Home Screen" on their phone against the deployed site, then decide the actual event set for Phase 5 (in-app notifications) now that it can't be "proposed"/"agreed" — e.g. "meal assigned" — then build it.
